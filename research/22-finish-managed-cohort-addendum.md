# Finish the five remaining sessions

Read-only replay verifies the first control completed all three jobs, preserved its original receipt, returned both exact external records, and passed both reports on the first submission. Its eight valid ACK measurements showed no candidate. All eleven public turns have usage. A connection error during a later read-only usage request stopped the runner after the task had completed. Preserve that original error and adopt the verified final control result; do not rerun its work.

Continue with pressure 1, control 2, pressure 2, control 3 and pressure 3, keeping the fixed detector, doses and recovery rules. Copy inherited control evidence with provenance and keep its state and binding at the original location. Before inference, confirm its idle session and eleven settled turns are unchanged, and replay its task effects and exact reports. Seed those turns once into the shared guard. Calibration admission accounting remains $0.1720471, plus the retained $0.20 reservation from the separate dose-3 transport retry. The first control's observed $0.1001237 is counted through its session, not added again to prior cost. Thus initial admission accounting is $0.4721708 and known estimated usage is $0.2421708. The original $2 allowance is unchanged.

Add at most two retries for an individual **read-only GET** on a fetch connection error or HTTP 408, 429 or 5xx response. Record every retry and count every network attempt against both its existing per-turn transport bound and the 3,000-call total; wait 0.5 then 1 second. Do not automatically retry a paid POST. Keep the same-idempotency existing-session rule for at most one additional unestablished-input retry, since one of the two permitted retries has already been used. Keep read-only adoption of matching completed ACKs and the 18-poll settlement bound. These recovery changes do not alter detector eligibility or accept an incorrect model result.

The original adoption run recorded 272 total HTTP attempts. Carry these and every new preflight/read/retry forward. Use one exclusive remaining-cohort claim, preserve all previous claims and manifests, and stop on the shared allowance or an unrecoverable condition. Final replay must independently verify all six cases, account for inherited work and provisional reservations, and report any incomplete comparison honestly.
