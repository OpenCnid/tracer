# Continue after a failed subscription

The same-session resumption completed baseline 2 with settled usage. Starting control dose 1 then returned `internal_error` before any SSE turn event. The collector explicitly cancelled and the final read-only snapshot showed the original idle session with exactly three completed turns (setup and two ACKs), all with usage. No new public dose turn was observed. The original failed attempt remains evidence.

Continue the user's authorized comparisons with the same session, binding, checkpoint, both baseline measurements and the exact saved dose-1 input. Use its original idempotency key `b1-control-dose-1`, preventing a duplicate input if a delayed write is discovered. Do not repeat calibration, setup or either baseline. Preserve the failed request and its timestamps. Retain an additional $0.20 admission reservation for that unestablished request; no new public turn is not a billing guarantee.

Before resuming, verify the session is still idle and unchanged, its three turns and usage match the recorded snapshot, and its original checkpoint is intact. Continue the unchanged detector, doses, correction rules, remaining five sessions and $2 total threshold. Prior HTTP count is 114; include every new read and request. Record the original $0.03 unknown Responses reservation separately from this $0.20 transport reservation. All existing known charges are carried once through calibration prior plus seeded managed turns.

Allow at most two further application-level retries for this specific recoverable condition across the remaining cohort: an existing-session `internal_error` with no new public turn, no required actions, idle state and complete prior usage, confirmed read-only after explicit cleanup. Reuse the exact input and idempotency key, retain a new $0.20 reservation for each unestablished attempt, and check the shared guard before retrying. SDK automatic retries remain disabled. Do not retry a model failure, invalid ACK, completed turn, task error, new unknown usage, or uncertain active work. Every attempt has separate evidence; a retry does not clear an earlier failure.

The extended usage settlement window stays at 18 read-only polls five seconds apart. Final replay joins inherited and new measurements and explicitly reports these interruptions. If infrastructure or the carried budget still prevents completion, preserve that limitation; no changed detector or extra pressure may turn it into a success.
