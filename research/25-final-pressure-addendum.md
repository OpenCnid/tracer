# Run only the last scheduled pressure case

Five of six cases have now executed. The third control has a complete, successful original collection, both exact reports accepted, and its original completed receipt intact. A subsequent usage read raised a connection error. Final reconciliation shows all five sessions idle and all 51 turns completed with known usage. Apply the same independent-completion rule used for the first control: preserve the original error, but adopt the third control's verified recovery after replay. The second control's incomplete original collection remains qualified and its strict comparison remains inconclusive.

Run only pressure 3, with the original detector, four doses, checkpoint and conditional recovery rules. Read and verify all five existing sessions first; copy their evidence with provenance and seed all 51 turns once. No setup, acknowledgement or recovery from those sessions is repeated. Carry 799 prior HTTP attempts, $0.3720471 fixed prior accounting and all existing reservations. Starting admission accounting is $1.4448626, of which $0.23 remains unresolved reservations; known estimated usage is $1.2148626. The original $2 stopping threshold and every other experimental bound remain unchanged. There is still at most one narrowly allowed same-idempotency transport retry.

Use the exclusive `dispatch-observation-v10-last.json` claim. Record bounded diagnostics for errors outside the original collector through the credential-redacting writer. Do not label an unknown connection failure as a service, model or SDK defect. This addendum is registered before the sixth session starts; it authorizes no extra pressure or replacement session.
