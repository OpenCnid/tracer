# Recover a completed turn after a stream error

The transport preflight found the dose-1 turn had appeared and completed after the earlier read-only snapshot. It refused to resubmit. The turn's user input matches the saved pending request byte-for-byte (SHA-256 `97a66064aa2b1a9802cae67306d22da397e8f38499886acb7e45b32d6a08577e`), its assistant output is ACK, and its usage is 8,106 input / 5 output tokens. No duplicate request was dispatched by that preflight.

Adopt this completed measurement using authoritative read-only turn/items resources, retaining the failed stream separately. Require a second identical usage observation, complete item history, idle session, no required actions, unchanged configuration, exact input match, and intact original checkpoint before more inference. This is the original dose 1, not a replacement or new baseline. Seed setup, both baselines and dose 1 once; continue at dose 2 in the same session and binding. All original doses and detector thresholds stay fixed.

The formerly unestablished request is now matched to its actual usage. Release its provisional $0.20 transport reservation and count the observed charge through the seeded session once. The original $0.03 unknown Responses reservation remains. The failed transport preflight made only three read-only calls; global HTTP accounting now starts at 117. A new exclusive adoption claim links the preceding attempts and retains the same total $2 allowance.

For the remaining run, apply this same read-only recovery to an ACK turn whose stream returns `internal_error`: if exactly one new completed public turn matches the requested input, has only the expected ACK output and no application work, preserve the original collection error and create a separately labeled adopted collection based on turn/items resources. Settle its usage before proceeding. This does not synthesize an SSE event; its completion basis is the public turn resource. If no new turn is visible, the previously bounded same-idempotency retry rule still applies. Never adopt a wrong answer, a model failure, mismatched input or task result. All other stopping rules remain.

Final evidence must distinguish original stream collection, later public visibility, adopted measurements, inherited measurements, new inference and provisional reservations. The delayed turn is itself a finding about managed recovery; it is not evidence of compaction.
